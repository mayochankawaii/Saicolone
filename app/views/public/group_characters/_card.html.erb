<div class="card">
  <%= link_to group_group_character_path(group, character), class: "card-link" do %>
  <%#= link_to group_group_character_path(group), class: "card-link" do %>
  <div class="card-img-character">
    <%#= image_tag character.image, class: "card-img-top", style: "max-width: 100%; flex-shrink:0;" %>
    <%= image_tag character.image, class: "card-img-index card-img-top" %>
  </div>
  <div class="card-body">
    <h6 class="card-title"><%= character.name %></h6>
    <p class="card-text"><%= Genre.find(character.genre_id).name %></p>
  </div>
  <% end %>
</div>